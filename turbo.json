{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "clean": {},
    "version": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "docs:build": {
      "outputs": ["website/.vitepress/dist/**"],
      "inputs": ["website/**", "packages/*/README.md"]
    }
  }
}