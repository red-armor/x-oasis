{
  "name": "@x-oasis/is-proxy-supported",
  "version": "0.8.0",
  "description": "Check if the runtime environment supports Proxy",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "module": "dist/is-proxy-supported.esm.js",
  "files": [
    "dist",
    "index.ts",
    "src"
  ],
  "scripts": {
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "clean": "rimraf ./dist",
    "test": "vitest",
    "compile": "tsc -p tsconfig.build.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsdx": "^0.14.1"
  }
}
