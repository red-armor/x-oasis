{
  "name": "@x-oasis/deep-clone",
  "version": "0.8.1",
  "description": "deep clone plain objects and arrays (preserves RegExp by reference)",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "module": "dist/deep-clone.esm.js",
  "files": [
    "dist",
    "index.ts",
    "src"
  ],
  "scripts": {
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "clean": "rimraf ./dist",
    "test": "vitest",
    "compile": "tsc -p tsconfig.build.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsdx": "^0.14.1"
  }
}
