{
  "name": "@x-oasis/web-stream",
  "version": "0.7.0",
  "description": "Web Streams API utilities: async-iterable bridging, SSE parsing, binary event stream decoding, and streaming tool call tracking",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "module": "dist/web-stream.esm.js",
  "files": [
    "dist",
    "index.ts",
    "src"
  ],
  "scripts": {
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "clean": "rimraf ./dist",
    "test": "vitest",
    "compile": "tsc -p tsconfig.build.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsdx": "^0.14.1"
  }
}
