{
  "name": "@x-oasis/push-stream",
  "version": "0.7.1",
  "description": "Single-consumer push-pull async stream with enqueue/done/error semantics (inspired by claude-code Stream<T>)",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "module": "dist/push-stream.esm.js",
  "files": [
    "dist",
    "index.ts",
    "src"
  ],
  "scripts": {
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "clean": "rimraf ./dist",
    "test": "vitest",
    "compile": "tsc -p tsconfig.build.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsdx": "^0.14.1"
  }
}
