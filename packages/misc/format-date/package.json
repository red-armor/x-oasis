{
  "name": "@x-oasis/format-date",
  "version": "0.12.0",
  "description": "tiny token-based date formatter (yyyy/MM/dd HH:mm:ss/sss)",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "module": "dist/format-date.esm.js",
  "files": [
    "dist",
    "index.ts",
    "src"
  ],
  "scripts": {
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "clean": "rimraf ./dist",
    "test": "vitest",
    "compile": "tsc -p tsconfig.build.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsdx": "^0.14.1"
  }
}
